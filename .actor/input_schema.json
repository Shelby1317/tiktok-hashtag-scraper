{
  "title": "TikTok Hashtag Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "mode": {
      "title": "Scraping Mode",
      "type": "string",
      "description": "Choose the type of hashtag scraping to perform",
      "default": "trending",
      "enum": ["trending", "search", "monitor"],
      "enumTitles": ["Trending Hashtags", "Search Specific Hashtags", "Monitor Hashtags"]
    },
    "hashtags": {
      "title": "Hashtags to Search/Monitor",
      "type": "array",
      "description": "List of hashtags to search or monitor (without # symbol). Only used in 'search' and 'monitor' modes.",
      "items": {
        "type": "string",
        "title": "Hashtag"
      },
      "default": []
    },
    "maxResults": {
      "title": "Maximum Results",
      "type": "integer",
      "description": "Maximum number of hashtags or videos to scrape",
      "default": 50,
      "minimum": 1,
      "maximum": 1000
    },
    "includeVideoDetails": {
      "title": "Include Video Details",
      "type": "boolean",
      "description": "Include detailed information about top videos for each hashtag",
      "default": true
    },
    "includeRelatedHashtags": {
      "title": "Include Related Hashtags",
      "type": "boolean",
      "description": "Include related hashtags for each main hashtag",
      "default": true
    },
    "includeSentimentAnalysis": {
      "title": "Include Sentiment Analysis",
      "type": "boolean",
      "description": "Analyze sentiment of comments (Pro feature)",
      "default": false
    },
    "includeInfluencers": {
      "title": "Include Top Influencers",
      "type": "boolean",
      "description": "Identify top influencers using each hashtag (Business feature)",
      "default": false
    },
    "outputFormat": {
      "title": "Output Format",
      "type": "string",
      "description": "Format for the output data",
      "default": "json",
      "enum": ["json", "csv"],
      "enumTitles": ["JSON", "CSV"]
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings for scraping",
      "prefill": {
        "useApifyProxy": true
      },
      "properties": {
        "useApifyProxy": {
          "type": "boolean",
          "title": "Use Apify Proxy",
          "description": "Use Apify's residential proxy service",
          "default": true
        }
      }
    }
  },
  "required": ["mode", "maxResults"]
}
